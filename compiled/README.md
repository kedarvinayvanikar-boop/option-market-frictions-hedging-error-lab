# Compiled Libraries

This folder holds the shared libraries built from `c_src/`. See
`c_src/build_instructions.md` for the exact commands.

Expected files on Linux:

- `libexample_add.so`
- `libbs_pricer.so`
- `libmonte_carlo.so`

These files are build artifacts and are not tracked in version control.
